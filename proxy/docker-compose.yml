services:

  nginx:
    image: nginx:alpine

    container_name: nginx-proxy

    ports:
      - "80:80"

    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./generated:/etc/nginx/generated

    networks:
      - proxy

networks:

  proxy:
    external: true